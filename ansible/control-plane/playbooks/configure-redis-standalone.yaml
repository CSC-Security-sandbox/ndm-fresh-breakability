---
- name: Configure data directory for Redis
  hosts: default
  gather_facts: yes
  become: yes
  roles:
    - { role: ../roles/configure-redis-standalone/redis-data, tags: 'redis-data' }

- name: Configure Redis
  hosts: default
  gather_facts: yes
  become: yes
  roles:
    - { role: ../roles/configure-redis-standalone/setup-redis, tags: 'setup-redis' }

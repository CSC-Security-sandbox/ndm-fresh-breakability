apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: postgres
type: Opaque
data:
  postgres-password: "{{ postgres_root_password_base64 }}"
  user-password: "{{ dmadmin_password_base64 }}"

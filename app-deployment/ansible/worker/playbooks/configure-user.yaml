---
- name: Configure user access on VM
  become: true
  become_method: ansible.builtin.sudo
  debugger: never
  gather_facts: true
  hosts: default
  roles:
    - { role:  ../roles/configure-user-account, tags: 'configure-user-account' }
apiVersion: v1
kind: Namespace
metadata:
  name: temporal
  labels:
    # Enable namespace-wide auto-injection for all Temporal pods
    # Schema Job has explicit sidecar.istio.io/inject: "false" to exclude it
    istio-injection: enabled
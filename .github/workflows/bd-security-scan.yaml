name: Black Duck SCA Scan with Vulnerability Count

on:
  push:
    branches: [main, sm/bd-action-workflow]
  pull_request:
    branches: [main, sm/bd-action-workflow]
  workflow_dispatch:

env:
  BLACKDUCK_URL: https://blackduck.eng.netapp.com
  PROJECT_NAME: Test_NDM
  VERSION_NAME: test_v
  BLACKDUCK_API_TOKEN: ${{ secrets.BLACKDUCK_API_TOKEN }}
  GITOPS_USER_GITHUB_TOKEN: ${{ secrets.GITOPS_USER_GITHUB_TOKEN }}

jobs:
  black-duck-sca-scan:
    name: Black Duck SCA Scan
    runs-on: openlab
    timeout-minutes: 120
    strategy:
      matrix:
        service:
          - name: admin-service
            path: services/admin-service
          - name: config-service
            path: services/config-service
          - name: db-writer
            path: services/db-writer
          - name: datamigrator-ui
            path: services/datamigrator-ui
          - name: jobs-service
            path: services/jobs-service
          - name: keycloak-customizations
            path: services/keycloak-customizations
          - name: reports-service
            path: services/reports-service

    defaults:
      run:
        working-directory: ${{ matrix.service.path }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://npm.pkg.github.com"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Replace GitHub Token Placeholder in .npmrc inside service path (if exists)
        if: ${{ matrix.service.name != 'keycloak-customizations' }}
        run: |
          sed -i 's|${GITOPS_USER_GITHUB_TOKEN}|${{ secrets.GITOPS_USER_GITHUB_TOKEN }}|' .npmrc || echo "⚠️ .npmrc not found, skipping replacement"

      - name: Install dependencies and pkg
        if: ${{ matrix.service.name != 'keycloak-customizations' }}
        run: npm install && npm install pkg -g

      - name: Run Build
        if: ${{ matrix.service.name != 'keycloak-customizations' }}
        run: npm run build

      - name: Validate environment
        run: |
          if [ -z "$BLACKDUCK_API_TOKEN" ]; then
            echo "❌ BLACKDUCK_API_TOKEN is not set. Please add it to GitHub Secrets."
            exit 1
          fi

          if [ -z "$GITOPS_USER_GITHUB_TOKEN" ]; then
            echo "❌ GITOPS_USER_GITHUB_TOKEN is not set. Please add it to GitHub Secrets."
            exit 1
          fi

          echo "✅ Environment validation passed"

      - name: Download Black Duck Detect
        run: |
          echo "📥 Downloading Black Duck Detect..."
          curl -s -L https://detect.blackduck.com/detect10.sh -o detect10.sh
          chmod +x detect10.sh
          echo "✅ Black Duck Detect downloaded successfully"

      - name: Run Black Duck Detect
        shell: bash
        env:
          BLACKDUCK_API_TOKEN: ${{ secrets.BLACKDUCK_API_TOKEN }}
          JAVA_OPTS: "-Dcom.blackduck.integration.trust.cert=true -Dtrust_cert=true -Dcom.blackduck.integration.verify.ssl=false -Xmx4g"
        run: |
          SERVICE_NAME="${{ matrix.service.name }}"
          SERVICE_PATH="${{ matrix.service.path }}"
          echo "🚀 Starting Black Duck scan for $SERVICE_NAME..."
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CODE_LOCATION_NAME="${PROJECT_NAME}-${SERVICE_NAME}-PR_${{ github.event.number }}"
          else
            CODE_LOCATION_NAME="${PROJECT_NAME}-${SERVICE_NAME}-${{ github.run_id }}"
          fi
          echo "Code location: $CODE_LOCATION_NAME"

          echo "CODE_LOCATION_NAME=$CODE_LOCATION_NAME" >> $GITHUB_ENV

          set +e
          ./detect10.sh \
            --detect.project.name="$PROJECT_NAME" \
            --blackduck.url="$BLACKDUCK_URL" \
            --blackduck.api.token="$BLACKDUCK_API_TOKEN" \
            --detect.project.version.name="$VERSION_NAME" \
            --detect.source.path="$SERVICE_PATH" \
            --detect.blackduck.signature.scanner.paths="$SERVICE_PATH" \
            --detect.code.location.name="$CODE_LOCATION_NAME" \
            --detect.cleanup=false \
            --detect.tools=DETECTOR,SIGNATURE_SCAN \
            --detect.detector.search.depth=25 \
            --detect.detector.search.continue=true \
            --blackduck.trust.cert=true \
            --blackduck.offline.mode=false \
            --detect.output.path="${{ github.workspace }}/blackduck-output/$SERVICE_NAME" \
            --detect.cleanup.bdio.files=false \
            --detect.accuracy.required=NONE \
            --blackduck.timeout=7200 \
            --detect.timeout=14400 \
            --detect.blackduck.signature.scanner.memory=4096 \
            --logging.level.com.synopsys.integration=INFO \
            --detect.wait.for.results=false \
            --detect.force.success=true \
            --detect.force.success.on.skip=true \
            --detect.bom.aggregate.remediation.mode=NONE \
            --detect.blackduck.scan.mode=INTELLIGENT \
            --detect.parallel.processors=4 \
            --detect.maven.dependency.types.excluded=false \
            --detect.npm.include.dev.dependencies=true \
            --detect.detector.search.exclusion.defaults=false \
            --detect.risk.report.pdf=false \
            --detect.notices.report=false \
            --detect.vulnerability.report=false

          SCAN_EXIT_CODE=$?

          echo "SCAN_EXIT_CODE=$SCAN_EXIT_CODE" >> $GITHUB_ENV

          case $SCAN_EXIT_CODE in
            0)
              echo "✅ SUCCESS: Black Duck scan completed successfully"
              ;;
            2)
              echo "⚠️ TIMEOUT: Scan timed out but data may have been uploaded"
              ;;
            *)
              echo "⚠️ Scan completed with exit code: $SCAN_EXIT_CODE"
              ;;
          esac

  upload-artifacts:
    name: Upload All Black Duck Reports
    runs-on: openlab
    needs: black-duck-sca-scan
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Upload all Black Duck Reports
        uses: actions/upload-artifact@v4
        with:
          name: blackduck-reports-all-${{ github.run_id }}
          path: blackduck-output/
          retention-days: 30

      - name: Final Summary
        if: always()
        run: |
          echo "📊 Black Duck Scan Final Summary for ${{ matrix.service.name }}:"
          echo "=================================="
          echo "Project: $PROJECT_NAME"
          echo "Version: $VERSION_NAME"
          echo "Service: ${{ matrix.service.name }} (${{ matrix.service.path }})"
          echo "Scan Exit Code: $SCAN_EXIT_CODE"
          echo "Scan Date: $(date)"
          echo ""
          echo "🔗 Black Duck Console: $BLACKDUCK_URL"
          echo "📄 Reports uploaded as GitHub artifacts"
          echo "📝 Note: Deep analysis data uploaded to Black Duck server."
          echo "📝 Full reports will be available in Black Duck web interface once server processing completes."

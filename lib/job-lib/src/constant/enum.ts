export enum GroupReaderType {
  WORKER = 'worker',
  DB_WRITER = 'db-writer',
}

export enum Streams {
    COMMANDS = 'commands',
    TASKS = 'tasks',
    ERRORS = 'ERRORS',
    FILES = 'files',
}

export enum HashSets {
    Tasks = 'tasks',
}
{
    "migrateConfigs": [
      {
        "sourcePathId":  "$sourcePathId",
        "destinationPathId": [#foreach($Id in $destinationPathId) "$Id"#if($foreach.hasNext),#end #end]
      }
    ],
    "preserveAccessTime": $preserveAccessTime
  }

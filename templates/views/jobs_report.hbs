<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <style type="text/css">
    table { page-break-inside:auto }
    tr    { page-break-inside:avoid; page-break-after:auto }
    thead { display:table-header-group }
    tfoot { display:table-footer-group }
  </style>
</head>
<body style="font-family: Arial, Helvetica, 'Lucida Grande', Geneva, Tahoma, Verdana, sans-serif; font-size: 12px;">
  <h1>{{title}}</h1>
  <body>
  <h1>{{'Jobs report'}}</h1>
  {{> customer_info }}
  {{> summary}}
  {{> current_iteration}}
  {{> last_iteration}}
  {{> last_errors}}
  {{> cutovers}}
  {{> coc}}
  {{> scan_iterations}}
  {{> aggregated_operations}}
</body>
</body>
</html>

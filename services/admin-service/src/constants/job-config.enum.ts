export enum JobStatus {
    Active = "ACTIVE",
    InActive = "IN_ACTIVE",
  }

export enum JobType {
    DISCOVER = "DISCOVER",
    SPEED_TEST = "SPEED_TEST",
    MIGRATE = "MIGRATE",
    CUT_OVER = "CUT_OVER",
    PRECHECK = "PRECHECK",
  }
